export default function LoginPage() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Login / Registration</h1>
      <p>Use NextAuth credentials provider (email + password).</p>
    </main>
  );
}
